
 Dear CompileJava users,

 CompileJava has been operating since 2013 completely free. If you
 find this site useful, or would otherwise like to contribute, then
 please consider a donation (link in 'More Info' tab) to support
 development of the new CompileJava website (stay tuned!).

 Most sincerely, Z.

import java.util.;
public class FindduplicatesInString{
  
  public static void main(String args[]){
 findDuplicateinString(Programming);
    
    
  }
  
  public static void findDuplicateinString(String String1){
    System.out.println(you are in the method);
    System.out.println(String1);
    char[] arr = String1.toCharArray();
     HashMapCharacter, Integer map = new HashMapCharacter,Integer();
    for(int i=0; iarr.length;i++){
      System.out.println(arr[i]);
      if(map.containsKey(arr[i])){
        map.put(arr[i], map.get(arr[i])+1);
        
      }
         else{
          map.put(arr[i], 1); 
         }
      
    }
     System.out.println(map);
    
    for(Map.EntryCharacter, Integer entry  map.entrySet()){
      
     System.out.println(entry);
      
      if(entry.getValue()  1){
        System.out.println(entry.getKey());
        
      }
      
      
    }
    
  
    
  }
  
}